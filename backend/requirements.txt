# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9     # 表单/文件支持（可选，预留）
# ORM（操作 SQLite）
sqlalchemy==2.0.35
# 数据校验
pydantic==2.9.2
# 配置读取
python-dotenv==1.0.1
# 测试（本地用，部署时可不装）
pytest==8.3.2
httpx==0.27.2
